{
  "name": "zachlevy-me-react-2",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && aws s3 sync build/ s3://zachlevy-me",
    "open": "open http://zachlevy-me.s3-website.ca-central-1.amazonaws.com/"
  }
}
